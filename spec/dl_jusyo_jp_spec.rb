require 'spec_helper'

describe DlJusyoJp do
  it 'has a version number' do
    expect(DlJusyoJp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
